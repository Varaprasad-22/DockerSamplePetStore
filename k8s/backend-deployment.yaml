apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: backend-petstore:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 4000
          env:
            - name: DB_URL
              value: "mongodb+srv://virupavaraprasad22:virupavaraprasad22@cluster0.mdtolec.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0"
